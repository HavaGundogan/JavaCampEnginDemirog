package classesWithAttribute;

public class ProductManager {
	public void Add(Product product) {
		System.out.println("ürün eklendi." + product.name);
	}// single responsibility

	/*
	 * Single responsibility prensibi sınıflarımızın iyi tanımlanmış tek bir
	 * sorumluluğu olması gerektiğini anlatmaktadır. Bir sınıf (nesne) yalnızca bir
	 * amaç uğruna değiştirilebilir, o amaçta o sınıfa yüklenen sorumluluktur, yani
	 * bir sınıfın yapması gereken yalnızca bir işi olması gerekir.
	 */

	//encapsulation

}
